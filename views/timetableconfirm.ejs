<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Timetable Added Successfully</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(to bottom, #f5f7fa, #e4e9f7);
        }

        .card {
            background: #ffffff;
            border-radius: 1rem;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
            max-width: 420px;
            margin: auto;
            padding: 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .checkmark {
            width: 100px;
            height: 100px;
            margin: auto;
            display: block;
            border-radius: 50%;
            background: linear-gradient(135deg, #22c55e, #16a34a);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
            transform: scale(0);
            animation: pop 0.5s ease-out forwards;
        }

        @keyframes pop {
            0% {
                transform: scale(0);
            }

            70% {
                transform: scale(1.2);
            }

            100% {
                transform: scale(1);
            }
        }

        .slide-up {
            animation: slideUp 0.6s ease-out forwards;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .btn-done {
            background: linear-gradient(90deg, #3b82f6, #2563eb);
            color: white;
            font-weight: 600;
            padding: 0.75rem 2rem;
            border-radius: 9999px;
            transition: all 0.3s ease-in-out;
        }

        .btn-done:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 20px rgba(59, 130, 246, 0.4);
        }

        .footer-text {
            color: #6b7280;
            font-size: 0.875rem;
        }
    </style>
</head>

<body class="min-h-screen flex items-center justify-center" ">

    <div class=" card slide-up">
    <!-- Checkmark -->
    <div class="checkmark mb-6">
        ✓
    </div>

    <h1 class="text-3xl font-bold text-gray-900 mb-2">Timetable Added!</h1>
    <p class="text-gray-600 mb-6">Your new timetable entry has been saved successfully.</p>

    <!-- DONE Button -->
    <form action="/facultytimetable" method="get">
        <button class="btn-done">
            DONE
        </button>
    </form>

    <!-- Footer -->
    <div class="footer-text mt-8">
        <p>Students will be automatically notified of the new timetable entry.</p>
        <p class="mt-1 text-gray-400 text-xs">Last updated: <strong>21 Sep, 2025</strong></p>
    </div>
    </div>

</body>

</html>